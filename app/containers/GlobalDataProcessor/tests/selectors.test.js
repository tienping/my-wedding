// import { fromJS } from 'immutable';
// import { selectGlobalDataProcessorDomain } from '../selectors';

describe('selectGlobalDataProcessorDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
